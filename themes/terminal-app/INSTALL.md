# Install (Terminal.app)

1. Open `Hanabi.terminal`.
2. Terminal.app imports it as a profile named `Hanabi`.
3. Go to `Terminal > Settings > Profiles` and select `Hanabi`.
4. Set as `Default` if desired.
