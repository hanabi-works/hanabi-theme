# Install (Terminal.app)

1. Open `Hanabi.terminal` (dark) and/or `Hanabi Light.terminal` (dark-palette alias).
2. Terminal.app imports profiles named `Hanabi` and `Hanabi Light`.
3. Go to `Terminal > Settings > Profiles` and select your preferred profile.
4. Set as `Default` if desired.
