# Install (iTerm2)

1. Open iTerm2.
2. Open `Settings...` (or `Preferences...` depending on your version).
3. Go to `Profiles` -> `Colors`.
4. Click `Color Presets...` -> `Import...`.
5. Select `hanabi.itermcolors`.
6. Click `Color Presets...` again and select `Hanabi`.

