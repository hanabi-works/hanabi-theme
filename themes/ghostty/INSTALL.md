# Install (Ghostty)

1. Copy `hanabi` to your Ghostty theme directory (typically `~/.config/ghostty/themes/`).
2. Open your Ghostty config (`~/.config/ghostty/config`).
3. Add or update: `theme = hanabi`.
4. Restart Ghostty.
